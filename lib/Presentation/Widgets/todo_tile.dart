import 'package:flutter/material.dart';

class ToDoTile extends StatelessWidget {
  const ToDoTile(this.title, this.description, {super.key});

  final String title;
  final String description;

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
